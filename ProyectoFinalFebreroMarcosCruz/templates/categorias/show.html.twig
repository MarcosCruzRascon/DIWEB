{% extends 'base.html.twig' %}

{% block title %}Categorias{% endblock %}

{% block body %}
    <h1>Categorias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categoria.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ categoria.descripcion }}</td>
            </tr>
        </tbody>
    </table>

     <a class="btn btn btn-outline-info float-left mr-5" href="{{ path('categorias_index') }}">Volver a la lista</a>

    <a class="btn btn-outline-warning float-right" href="{{ path('categorias_edit', {'id': categoria.id}) }}">Editar</a>

    {{ include('categorias/_delete_form.html.twig') }}
{% endblock %}
