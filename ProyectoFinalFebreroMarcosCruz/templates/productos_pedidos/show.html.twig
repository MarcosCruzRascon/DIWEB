{% extends 'base.html.twig' %}

{% block title %}ProductosPedidos{% endblock %}

{% block body %}
    <h1>ProductosPedidos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productos_pedido.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('productos_pedidos_index') }}">back to list</a>

    <a href="{{ path('productos_pedidos_edit', {'id': productos_pedido.id}) }}">edit</a>

    {{ include('productos_pedidos/_delete_form.html.twig') }}
{% endblock %}
