{% extends 'base.html.twig' %}

{% block title %}Pedidos
{% endblock %}

{% block body %}
	<h1>Pedidos</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ pedido.id }}</td>
			</tr>
			<tr>
				<th>Idpedido</th>
				<td>{{ pedido.idpedido }}</td>
			</tr>
			<tr>
				<th>Anotaciones</th>
				<td>{{ pedido.anotaciones }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn btn-outline-info float-left mr-5" href="{{ path('pedidos_index') }}">Volver al listado</a>


	<a class="btn btn btn-outline-info align-items-center" href="{{ path('muestrainfopedido', { 'id': pedido.id }) }}">Ver productos incluidos</a>


	<button type="button" class="btn btn-outline-danger float-right" data-toggle="modal" data-target="#myModal">Borrar</button>
	{{ include('pedidos/_delete_form.html.twig') }}
{% endblock %}
